{
  "name": "simple-test",
  "version": "1.0.0",
  "scripts": {
    "start": "node dist/app.js",
    "build": "mkdir -p dist && cp app.js dist/"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
